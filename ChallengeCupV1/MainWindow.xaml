<Window x:Class="ChallengeCupV1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChallengeCupV1"
        xmlns:view="clr-namespace:ChallengeCupV1.View"
        xmlns:data="clr-namespace:ChallengeCupV1.DataSource"
        xmlns:d3="http://research.microsoft.com/DynamicDataDisplay/1.0"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <TabControl Grid.Row="0" Margin="5,5,5,0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Height" Value="30"/>
                    <Setter Property="Width" Value="60"/>
                </Style>
            </TabControl.Resources>
            <TabItem Header="Wave" Name="waveTab">
                <TabItem.Resources>
                    <data:Data x:Key="dataSource"/>
                </TabItem.Resources>
                <Grid>
                    <UniformGrid Rows="2" Columns="2">
                        <!--<Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>-->
                    <view:Wave x:Name="wave1" 
                               Margin="10,10,20,10" Grid.Column="0" Grid.Row="0"/>
                    <view:Wave x:Name="wave2" 
                               Margin="10,10,20,10" Grid.Column="1" Grid.Row="0"/>
                    <view:Wave x:Name="wave3" 
                               Margin="10,10,20,10" Grid.Column="0" Grid.Row="1"/>
                    <view:Wave x:Name="wave4" 
                               Margin="10,10,20,10" Grid.Column="1" Grid.Row="1"/>
                    </UniformGrid>
                </Grid>
                
                <!--<d3:ChartPlotter x:Name="plotter" Margin="10,10,20,10" DataContext="{StaticResource dataSource}">
                    <d3:ChartPlotter.VerticalAxis>
                        <d3:VerticalIntegerAxis />
                    </d3:ChartPlotter.VerticalAxis>
                    <d3:ChartPlotter.HorizontalAxis>
                        <d3:HorizontalIntegerAxis />
                    </d3:ChartPlotter.HorizontalAxis>
                    <d3:Header Content="Title"/>
                    <d3:VerticalAxisTitle Content="Y"/>
                    <d3:LineGraph x:Name="line" DataSource="{Binding Points}"/>
                </d3:ChartPlotter>-->
            </TabItem>

            <TabItem Header="Gear" Name="gear">

            </TabItem>

            <TabItem Header="Status" Name="status">

            </TabItem>
        </TabControl>
        <view:FunctionColumn Grid.Row="1"/>
    </Grid>
</Window>
